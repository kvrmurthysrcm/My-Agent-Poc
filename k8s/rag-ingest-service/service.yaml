apiVersion: v1
kind: Service
metadata:
  name: rag-ingest-service
  namespace: rag-poc
spec:
  type: ClusterIP
  selector:
    app: rag-ingest-service
  ports:
    - name: http
      port: 8000
      targetPort: http
