apiVersion: v1
kind: Service
metadata:
  name: online-library-mcp
  namespace: rag-poc
spec:
  selector:
    app: online-library-mcp
  ports:
    - name: http
      port: 8004
      targetPort: 8004

