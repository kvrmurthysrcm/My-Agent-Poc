apiVersion: v1
kind: Service
metadata:
  name: rag-search-service
  namespace: rag-poc
spec:
  type: ClusterIP
  selector:
    app: rag-search-service
  ports:
    - name: http
      port: 8001
      targetPort: 8001
