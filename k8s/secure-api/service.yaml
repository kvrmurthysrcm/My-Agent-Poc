apiVersion: v1
kind: Service
metadata:
  name: secure-api
  namespace: rag-poc
spec:
  type: ClusterIP
  selector:
    app: secure-api
  ports:
    - name: http
      port: 8010
      targetPort: http
