# Ingestion and Admin Prompt

Review the authorized ingest and resource-admin features. Preserve multipart upload (never manually set Content-Type), full existing metadata/chunk/index modes, local metadata preview, job polling/errors, retry/reindex/delete confirmation, and Graph RAG settings. Confirm every operation goes through a role-protected gateway route and document unavailable server capabilities instead of fabricating controls.
