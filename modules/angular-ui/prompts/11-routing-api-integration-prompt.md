# Routing and API Integration Prompt

Review all lazy routes, functional guards, route role data, typed services, interceptors, proxy configuration, and DTO mappings. Ensure every browser request remains gateway-relative, no credentials are sent to external URLs, each feature has loading/error/empty behavior, and backend contract gaps are explicitly documented rather than hidden.
