# Repository Analysis Prompt

Analyze the complete My-Agent-Poc repository before changing code. Inspect gateway routes, downstream schemas, browser HTML/JS, roles, authentication, SSE, uploads, catalog, MCP, and agent APIs. Update `docs/EXISTING_UI_FUNCTIONALITY_INVENTORY.md` and `docs/API_ENDPOINT_MAPPING.md` with evidence. Do not invent endpoints or expose internal credentials. Identify every capability gap between browser requirements and secured gateway routes.
