[pytest]
filterwarnings =
    # Starlette 1.6.0 still imports AnyIO's compatibility alias. Keep this
    # narrowly scoped until the upstream TestClient switches to the new path.
    ignore:The anyio\.abc\.BlockingPortal alias is deprecated.*:DeprecationWarning:starlette\.testclient
